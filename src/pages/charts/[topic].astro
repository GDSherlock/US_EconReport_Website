---
import Page from '../../components/pages/ChartTopicPage.astro';
import {topics} from '../../lib/chart-topics';
export function getStaticPaths(){return topics.map(topic=>({params:{topic:topic.id},props:{topicId:topic.id}}));}
---
<Page locale="zh" {...Astro.props} />
